# Core dependencies
markitdown[all]>=0.5.0
flask>=3.0.0
flask-cors>=4.0.0

# Build dependencies
pyinstaller>=6.0.0

# Note: Install with uv for faster dependency resolution:
# uv pip install -r requirements.txt